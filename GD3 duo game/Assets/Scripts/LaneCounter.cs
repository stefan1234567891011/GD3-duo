using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LaneCounter : MonoBehaviour
{
    // Start is called before the first frame update
    public int Counter = 0;
    void Start()
    {
        Counter = 0;
    }
}
